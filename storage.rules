rules_version = '2';

// All access is performed server-side via the Firebase Admin SDK, which
// bypasses these rules. Deny all direct client access by default.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
